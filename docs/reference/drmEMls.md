# EM algorithm for least squares

EM algorithm for least squares

## Usage

``` r
drmEMls(
  dose,
  resp,
  multCurves,
  startVec,
  robustFct,
  weights,
  rmNA,
  dmf = NULL,
  doseScaling = 1,
  respScaling = 1,
  varcov = NULL
)
```
