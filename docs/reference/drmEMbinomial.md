# EM algorithm for binomial response

EM algorithm for binomial response

## Usage

``` r
drmEMbinomial(
  dose,
  resp,
  multCurves,
  startVec,
  robustFct,
  weights,
  rmNA,
  zeroTol = 1e-12,
  doseScaling = 1,
  respScaling = 1
)
```
