# Create model evaluation function

Create model evaluation function

## Usage

``` r
modelFunction(
  dose,
  parm2mat,
  drcFct,
  cm,
  assayNoOld,
  upperPos,
  retFct,
  doseScaling,
  respScaling,
  isFinite,
  pshifts = NULL
)
```
