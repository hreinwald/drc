# Generate parameter names for drm

Generate parameter names for drm

## Usage

``` r
drmParNames(
  numNames,
  parNames,
  collapseList2,
  repStr1 = "factor(pmodels[, i])",
  repStr2 = "factor(assayNo)"
)
```
