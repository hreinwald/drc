# Construct Text for Model with Fixed Lower Limit

Helper function that appends lower limit information to a model
description string.

## Usage

``` r
lowFixed(modelStr)
```

## Arguments

- modelStr:

  character string with the base model description.

## Value

A character string describing the model with its fixed lower limit.
